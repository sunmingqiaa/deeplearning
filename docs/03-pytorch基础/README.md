# 03 PyTorch 基础

目标是掌握 PyTorch 的标准训练流程。

## 重点主题

- `torch.Tensor`
- `requires_grad`
- `autograd`
- `torch.nn.Module`
- `Dataset` 和 `DataLoader`
- `optimizer`
- 训练循环和评估循环

## 最小验收

- 能独立写一个完整训练循环。
- 能解释 `zero_grad()`、`backward()`、`step()` 的顺序。
- 能保存和加载模型参数。
